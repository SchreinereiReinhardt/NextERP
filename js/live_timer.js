document.addEventListener('DOMContentLoaded',()=>{const card=document.querySelector('.erp-live-timer');const out=document.getElementById('erp-live-duration');if(!card||!out)return;const start=new Date(String(card.dataset.start).replace(' ','T')).getTime();const pause=Number(card.dataset.pauseSeconds||0)*1000;const status=card.dataset.status;const pausedAt=card.dataset.pausedAt?new Date(String(card.dataset.pausedAt).replace(' ','T')).getTime():null;const tick=()=>{const end=status==='paused'&&pausedAt?pausedAt:Date.now();let ms=Math.max(0,end-start-pause);const sec=Math.floor(ms/1000),h=Math.floor(sec/3600),m=Math.floor((sec%3600)/60),s=sec%60;out.textContent=[h,m,s].map(v=>String(v).padStart(2,'0')).join(':')};tick();setInterval(tick,1000)});