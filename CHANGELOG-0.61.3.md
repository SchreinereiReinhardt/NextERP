# NextERP 0.61.3

- Dokument-Prüfseite auf eine eindeutige Route `/document-review/{id}` umgestellt.
- 404 beim Klick auf „Zuordnen“ behoben.
- Upload leitet direkt zum Prüf-Arbeitsplatz weiter.
- Alte Route `/documents/{id}` bleibt aus Kompatibilitätsgründen erhalten.
