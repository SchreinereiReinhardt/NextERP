# NextERP 0.66.2

- Mobile API project listing now recognizes the `administrator` role.
- Administrators no longer require an entry in `re_erp_project_users` to see active projects.
- Dashboard `projectsToday` and `recentProjects` now use the corrected project visibility.
